// The fragment block replaces itself with the fetched page's sections.
// There is no fixed wrapper template — the loaded content is inserted directly.
// This file documents the expected authored input structure for reference.

export const FRAGMENT_MARKUP = /* html */`
<a href="{path}">{path}</a>
`;

export default FRAGMENT_MARKUP;
